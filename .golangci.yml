version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - misspell
    - staticcheck
  settings:
    staticcheck:
      checks:
        - all
        - -QF1012
        - -S1016
        - -ST1000

formatters:
  enable:
    - gofmt
